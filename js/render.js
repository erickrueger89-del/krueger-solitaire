window.KID = window.KID || {};

window.KID.Render = {
    draw(state) {
        console.log(
            "Rendering",
            state.tableau.length,
            "tableau columns."
        );
    }
};